# Direct dependencies only; transitive packages resolve on their own.
# Lower bounds rather than exact pins so the app builds on current Python --
# the old pins (psycopg2-binary 2.9.9, greenlet 3.0.3) fail to compile on 3.13+.
Flask>=3.0.2
Flask-Login>=0.6.3
Flask-Migrate>=4.0.5
Flask-SQLAlchemy>=3.1.1
Flask-WTF>=1.2.1
SQLAlchemy>=2.0.27
Werkzeug>=3.0.1
python-dotenv>=1.0.1
gunicorn>=21.2.0

# The app stores analytics in SQLite (stdlib). Reinstate psycopg2-binary here
# if DATABASE_URL is ever pointed at PostgreSQL.
